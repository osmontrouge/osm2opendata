name: Toilettes publiques
overpass: |
    [out:json][timeout:25];
    // TODO: Refine indoor / access
    {{geocodeArea:Montrouge}}->.searchArea;
    (
        nwr["amenity"="toilets"](area.searchArea);
    );
    out body;
    >;
    out skel qt;
