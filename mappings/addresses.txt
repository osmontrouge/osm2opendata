description: Base Adresse Locale
overpass: |
    [out:json][timeout:25];
    // TODO: Raffiner
    {{geocodeArea:Montrouge}}->.searchArea;
    (
        nwr["addr:housenumber"](area.searchArea);
    );
    out body;
    >;
    out skel qt;
