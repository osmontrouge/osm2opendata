name: Bars et pubs
overpass: |
    [out:json][timeout:25];
    {{geocodeArea:Montrouge}}->.searchArea;
    (
        nwr["amenity"="bar"](area.searchArea);
        nwr["amenity"="pub"](area.searchArea);
    );
    out body;
    >;
    out skel qt;
