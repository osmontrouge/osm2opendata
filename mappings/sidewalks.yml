name: Emprise des trottoirs et espaces dédiés aux piétons
overpass: |
    [out:json][timeout:25];
    {{geocodeArea:Montrouge}}->.searchArea;
    (
        nwr["highway"="footway"]["footway"="sidewalk"](area.searchArea);
        nwr["highway"="pedestrian"]["area"="yes"](area.searchArea);
    );
    out body;
    >;
    out skel qt;
