name: Micro-bibliothèques de rue
overpass: |
    [out:json][timeout:25];
    {{geocodeArea:Montrouge}}->.searchArea;
    (
        nwr["amenity"="public_bookcase"](area.searchArea);
    );
    out body;
    >;
    out skel qt;
